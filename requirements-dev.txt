# When bumping ruff/black, run the full pytest suite locally first, then
# `make lint-fix` to normalise any newly-caught style drift in its own commit
# before merging the bump (matches the CI lint-and-test gate in Makefile).
pytest>=9.1.1
ruff>=0.15.22
black>=26.5.1
httpx>=0.28
pytest-asyncio>=0.23
hypothesis>=6.161.5
pyyaml>=6.0  # scripts/gen_openapi.py --check / regen (tests/test_gen_openapi.py)
# sdk/mcp_server.py + tests/test_mcp_server_security.py (T27)
# Pinned <2: mcp v2.0.0 (2026-07-28) removed Tool.inputSchema + Server.list_tools decorator; migration is separate work.
mcp>=0.9,<3
web3>=6.20,<7  # T3.4-B Sepolia HTLC bridge: server/bridge_evm_adapter.py, scripts/deploy_htlc_sepolia.py,
               # demo/bridge_e2e_showcase.py, tests/test_bridge_evm_sepolia_integration.py (network-marked,
               # deselected by default). eth-account/eth-utils below are pulled in transitively by web3 —
               # do not pin eth-account>=0.13, web3 6.20.x requires eth-account<0.13.
eth-account>=0.8,<0.13
eth-utils>=4.0
