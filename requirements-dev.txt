# When bumping ruff/black, run the full pytest suite locally first, then
# `make lint-fix` to normalise any newly-caught style drift in its own commit
# before merging the bump (matches the CI lint-and-test gate in Makefile).
pytest>=9.1.1
ruff>=0.15.22
black>=26.5.1
httpx>=0.28
pytest-asyncio>=0.23
hypothesis>=6.161.5
pyyaml>=6.0  # scripts/gen_openapi.py --check / regen (tests/test_gen_openapi.py)
